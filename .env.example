# Capyy local overrides (optional).
#
# Provider credentials, provider URLs, models, priority and account tokens are
# configured in Dashboard and stored in the gateway-config volume. Keep this
# file free of provider secrets.
